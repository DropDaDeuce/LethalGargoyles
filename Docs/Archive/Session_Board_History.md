# Session Board History

Released claim rows moved off [`Docs\Session_Board.md`](../Session_Board.md) so the live board stays short enough to read in one go.

**Rows land here VERBATIM.** Trimming the board is a MOVE, never a delete — these rows carry rulings, measured findings and "why it looks like this" notes that exist nowhere else. Add a dated section header for each groom, newest at the top, and keep the original table shape inside it.

**Nothing here is open work.** If a row here still sounds like something owed, it is a bookkeeping failure, not a task — check the code before acting on it.

---

## Groomed 2026-08-16 (during board b28)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| cornered-aggression (AI / state machine) | 18 | Plugin\src\Enemy\LethalGargoylesAI.cs, Plugin\src\Config\Configuration.cs, CHANGELOG.md, Docs\Session_Board.md | 2026-08-16 — **Mathew's ask, from watching it pace in an open stairwell: give the Gargoyle an exit when there is nowhere to hide.** Debug AND Release both 0 warnings / 0 errors. **NOT RUN.** New `General > Cornered Aggression Delay` (default `5`, `0` disables): watched continuously for that long while still failing to hide, he abandons hiding and commits to `AggressivePursuit`. **The design decision that matters, and the reason this also cures the pacing:** the timer keys on UNBROKEN line of sight, **not** on the hide search reporting failure. In an open room the search never reports failure - it returns the least-bad of several equally exposed spots every time, which is exactly what produces the pacing. "Visible for N seconds straight" is the symptom that is true in both the can't-find-cover and the found-nothing-good cases, and it self-clears the instant he reaches cover. **Two things a later session must not remove:** (1) `_corneredAggroUntil` (8s, `CORNERED_AGGRO_COMMIT`) and its early-return guard at the top of `HandleOutOfAggroRange` - without it that method's `isSeen` arm fires on the very next tick, since the player is of course still looking at him, and the escalation would last one frame; (2) the falling edge of `isSeen` clears BOTH `_seenSinceTime` and `_corneredAggroUntil` - breaking line of sight is the outcome the escalation exists to reach, so reaching it must not leave him aggressive. **Reported and deliberately NOT fixed:** the Gargoyle pathed *inside a storage cabinet* looking for cover. Cover points come from `NavMesh.SamplePosition` around AI nodes, which only proves a point is on the navmesh, not that it is in a sensible place. A `Physics.CheckSphere` clearance filter would reject those - but it would also reject legitimate tight cover, and **making cover scarcer is the exact problem this batch just fixed**, so it needs a playtest first, not a blind guard. |

---

## Groomed 2026-08-16 (during board b26)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| changelog-v080 (docs / planning) | 16 | CHANGELOG.md, Docs\Session_Board.md, Docs\Archive\Session_Board_History.md | 2026-08-16 — **`## Unreleased` rewritten in the `changelog-writer` voice and renamed `## v0.8.0 - The Wandering Gargoyle`, at Mathew's call. No source touched, no build run.** The rewrite was not cosmetic: **the old section was missing every player-facing change from b8, b9 and b13** and covered only b11/b12/b14, so it would have shipped a release note omitting the audio-pipeline fixes, the features that never worked, the stutter fix and both statue fixes. An `Explore` sweep verified all ten candidate items against the working tree before a word was written, and **it killed two lines that would have been false**: (1) round-end cleanup is **PARTIAL** — `RemoveSelfFromSharedState` on despawn/destroy is what fixes the permanently-un-shovable player, but `ClearAllVariables` still has exactly one caller (the crew-wipe branch) and the two round-end Harmony hooks in `AIHelperPatches.cs` are still commented out, so the changelog claims only the push fix; (2) the stereo decode fix is **client-side only** (the host loads via `DownloadHandlerAudioClip` and was never affected), so the bullet says "for everyone except the host". C3 (push/damage owner authority) confirmed still unfixed and deliberately unmentioned. **New `**Performance:**` group header added** — the only new one, and it earned it: two bullets that were miscategorised as AI behaviour. **DEFECT FOUND WHILE VERIFYING, NOT FIXED, NOT IN THE CHANGELOG — see *Open defects*: `FoundClosestPlayerInRange` counts a gargoyle's own entry against its current target before clearing it, so with `fairShare == 1` a gargoyle that drops a target can never re-acquire that same player.** Suspected solo-play blocker for v0.8.0. **Version numbers NOT bumped** (csproj + `manifest.json` still `0.7.0`); parked as a Pending manual step. |

---

## Groomed 2026-08-16 (during board b25)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| changelog-skill (docs / planning) | 15 | .claude\skills\changelog-writer\SKILL.md (NEW), .claude\skills\ship-release\SKILL.md, CLAUDE.md, Docs\INDEX.md, Docs\Session_Board.md, Docs\Archive\Session_Board_History.md | 2026-08-16 — **new `changelog-writer` procedure skill; no source touched, no build run, `CHANGELOG.md` itself deliberately NOT edited.** Fires on ANY write to `CHANGELOG.md`, from a one-bullet batch append up to a full release. Carries three things that were being re-derived every time: the bullet shape (**plain sentence a non-coder understands first, the mechanism second, the consequence third** — one bullet serves both audiences instead of splitting the file), the reusable group headers, and an explicit ban list for the punctuation and phrasing that make generated text obvious (em/en dashes, curly quotes, chiasmus, rhetorical triads, filler vocabulary). Also encodes the four facts that must be stated when true: **unverified in game**, **existing cfg files do not pick up new defaults**, behaviour changes that will read as bugs, and off-by-default. **`ship-release`'s changelog section was gutted to a pointer** rather than duplicated, keeping only the two release-specific rules (themed version heading, rename `Unreleased` rather than write fresh). **Known tension, flagged to Mathew and NOT acted on: the existing `## Unreleased` section is full of em dashes**, so it matches the new skill on structure and altitude but violates its punctuation rules. Cleaning it is a `CHANGELOG.md` edit and needs its own claim. |

---

## Groomed 2026-08-16 (during board b22 and b23)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| taunt-rate-and-cfg-order (config, AI / state machine) | 14 | Plugin\src\Config\Configuration.cs, Plugin\src\Enemy\LethalGargoylesAI.cs, CHANGELOG.md, Docs\Session_Board.md | 2026-08-16 — **`BUILD` RELEASED. Debug AND Release both 0 warnings / 0 errors. NOTHING HAS BEEN RUN.** Mathew asked for a lever to increase the time between taunts and to move the audio toggles to the bottom of the config. **The two asks had the same root cause and the first lever already existed.** `General > Min Taunt` (15) / `Max Taunt` (45) drive every taunt path — `MarkTaunted`, `OtherTaunt` and `EnemyTaunt` all call `Random.Range(minTaunt, maxTaunt)` unmodified. He could not find them because **BepInEx's `ConfigFile.Save` sorts sections alphabetically**, so the ~830 `Audio.*` per-clip toggles filled lines 4–848 of the live 1050-line cfg and `[General]` did not start until line 939. Verified empirically off the Dev-profile cfg, not inferred. **Applied:** (1) section renamed `Audio.<Cat>` â†’ `Voice Lines.<Cat>` (`AudioSectionPrefix` const) — **V sorts after Diagnostics/General/Pathfinding/Scrap, and that is the ONLY reason for the name; a future section starting W–Z lands below the toggles and is just as buried**; (2) a **one-time value migration** reads the old values out of BepInEx's private `OrphanedEntries` *before* `ClearUnusedEntries` deletes them, so clips a player had switched off stay off — without it the rename would silently re-enable everything with nothing in the log. The reflection is now shared via `TryGetOrphanedEntries`, which returns null and warns instead of throwing; (3) `General > SteamID Taunt Cooldown` (default `90f`) replaces the bare `90f` literal in `TryPlayPlayerSpecificTaunt` — the only taunt timer with no knob. `Start`'s `Time.time - 91f` backdate now tracks the config value, or raising the cooldown would re-gate the first personal line; (4) corrected the Min/Max descriptions, which claimed "other types of taunts will be half this number" — never true in any version of the code. **MATHEW'S RULING, do not silently "fix" it:** `HandleAggroAndPush` sets `randAgrTauntTime = Time.time - lastAgrTauntTime` when a player leaves aggro range, making the gate `X >= X` and always true, so **every re-aggro barks instantly and bypasses Min Taunt**. He was shown this and chose to LEAVE IT AS-IS — the immediate bark is the tell that he is charging. It is documented in the Min Taunt description, not changed. **Also noted, unchanged:** `minTaunt`/`maxTaunt` are `int`, so `Random.Range` is max-EXCLUSIVE — 30/60 really draws 30–59. **Mathew edited the DEFAULTS himself mid-batch, 15/45 â†’ 30/60** — that is a shipped behaviour change, recorded in `CHANGELOG.md` under *Tuning*, and it does NOT reach anyone who already has a cfg on disk. |
| playtest-followups (AI / state machine) | 12 | Plugin\src\Enemy\LethalGargoylesAI.cs, Plugin\src\Config\Configuration.cs, CHANGELOG.md, Docs\Session_Board.md | **`BUILD` RELEASED — Debug AND Release both green (0/0).** Acting on Mathew's b11 playtest. **`BUILD` RE-TAKEN after the deep-debug landed.** Earlier note: **`BUILD` RELEASED 2026-08-16** — Debug is green (0/0) and b13 may need it; will re-take it if more edits land. **Applied, confirmed cause:** push-state footstep volume (`creatureSFX` IS the footstep source — `LethalGargoylesSFX.PlayStep` PlayOneShots through it — and PushTarget ran at 1.7, same as an open chase), and the up-to-3s reaction to being seen (four timers stack: 0.33s seen-check + **2.0s `pathDelayTimer` commitment gate** + 0.35s hide eval + 0.75s repath; the rising edge of `isSeen` now expires all three rather than shortening any, which would bring back the dithering they prevent). **Added instrumentation, not fixes, for his other three reports** — `LogCat.Movement` @ Debug now names which hide branch ran, the distance, and specifically the `ChooseClosestNodeToPos` -> own-position stuck case. **KILLED by his test: the client-replication theory** — identical behaviour on host and client, so `updatePositionThreshold` is NOT the cause. His read is "a distance thing", which fits the hard `distanceToPlayerSqr > idleDistanceSqr` cutover at exactly 20m. `deep-debug` still running. |

---

## Groomed 2026-08-16 (during board b19)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| optimize-pass-exec (AI + audio + scrap + config + build) | 8 | Plugin\src\Utility\LGLog.cs (NEW), Plugin\src\Enemy\LethalGargoylesAI.cs, Plugin\src\Utility\AudioManager.cs, Plugin\src\Scrap\GargoyleStatue.cs, Plugin\src\Config\Configuration.cs, Plugin\src\LethalGargoyles.cs, Docs\Plans\Optimization_Audit.md, Docs\Session_Board.md | 2026-08-15 — **executed batches A, E(partial), F, and C/D/G(partial) of the b5 audit autonomously at Mathew's explicit direction while he was away; he accepted the risk after it was raised. FIVE COMMITS, one per batch, so any single batch reverts alone** (`61cc313` A, `0a9ff00` E, `3f92b72` F, `a1dac4b` C/D/G). **EVERY COMMIT IS COMPILE-VERIFIED ONLY — 0 warnings / 0 errors in Debug AND Release — and NOT ONE LINE HAS BEEN RUN.** Release was built deliberately, not as a formality: it is the only configuration where the new `#if DEBUG` blocks are actually excluded. **The two decisions a later session must not silently undo:** (1) **Batch B, the partial-class split, was SKIPPED** — it would have eaten the remaining budget rewriting 2,900 lines for zero behaviour gain, and its whole purpose was to make *later* diffs readable while the later diffs were happening first. It is now a BIGGER job than b5 estimated, since four commits landed on top. (2) **C3, the push/damage owner-authority fix, was deliberately NOT applied** — it is the only top-tier finding still open, it is the one marked PLAUSIBLE rather than CONFIRMED, and it rewrites how damage reaches players. Shipping an unverified rewrite of the damage path with nobody able to test was the worse bet. **Found while fixing E2 and NOT in the original audit:** `GetTargetPosition` picked `targetZone` before the left/right decision, so once the distances actually differed the "left wins" branch returned the RIGHT zone — unreachable only because E2 kept both distances equal, so fixing E2 alone would have swapped one bug for another. Both went in together. Also renamed the new `LogLevel` enum to `LGLevel` (collided with `Unity.Netcode.LogLevel`) and dropped a `using System;` from the AI file (made `Random` ambiguous against `UnityEngine.Random`, which five sites use unqualified). |

---

## Groomed 2026-08-16 (during board b17)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| thumbnail (build + packaging) | 6 | icon.png, AssetSources\Images\thumbnail_build.py (NEW), AssetSources\Images\Thumbnails\ (NEW) | 2026-08-15 — **new Thunderstore icon, composited from the real model render rather than generated.** `icon.png` is now the `D_stone` candidate: the gargoyle from `Screenshot_3448-Photoroom.png` (which already carries a clean alpha channel) over a blurred crop of the AI background's corridor *floor*. **That crop box `(456, 400, 1080, 1024)` is load-bearing — it sits below every piece of AI-rendered text in the source image,** including the misspelled "CATION" sign and the "LETHAL COMPANY" wordmark, which is why the result has no AI tell. The old icon's real defect was contrast, not provenance: a dark creature on a dark background with no rim separation, unreadable at the 64-128px that Gale and r2modman actually render. `thumbnail_build.py` regenerates all four candidates plus a `_64x.png` legibility proof for each; tuning knobs (`SC`, `EY`, `ES`, `silhouette=`) are documented in its docstring. **Packaging only — no `.cs` touched, no build run, no `gargoyleassets` rebuild. It reaches players only on the next Release build.** |

---

## Groomed 2026-08-16 (during board b16)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| optimization-audit (docs / planning) | 5 | Docs\Plans\Optimization_Audit.md (NEW), Docs\INDEX.md, Docs\Session_Board.md | 2026-08-15 — **phase 1 of the improve/optimize pass: a read-only audit of all of `Plugin\src\**`, ~70 findings, NOTHING applied.** Full record in [`Optimization_Audit.md`](Plans/Optimization_Audit.md) — read its *headline* table first. **The finding that reframes the whole pass: this is not primarily a performance problem — NINE FEATURES DO NOT WORK, or work only for the host.** Target balancing is dead (`gargoyleTargets[myID]` is never written on acquisition, so every gargoyle stalks the same player and `ChangeTarget` is unreachable dead code); `CheckZonePath` resets *both* distances at the top of every call, so left/right circling has never worked; the statue's `ItemActivate` gates a `RequireOwnership = false` ServerRpc behind `IsServer`, muting it for non-hosts; push damage is applied server-side to remote players' copies (PLAUSIBLE — **invisible to Mathew because he tests as host**); `LGInstance` gating means one gargoyle's death stops ALL survivors closing doors; stereo OGGs decode to half length and one shipped clip is stereo; and static state is never cleared when the crew *survives*, leaving a player permanently un-pushable for the session. **Two findings were corroborated by independent agents (the statue `IsServer` bug, the `randomIndex++` overflow) — the safest bets in the doc.** Settled two open `ToDo.md` questions with evidence: **Concentus is confirmed unused** (zero hits across `Plugin\`), and the seven-place category table is currently in sync. **Verified clean, recorded so nobody re-audits it:** zero soft-dep leakage, zero Harmony prefixes, no patch-target drift (all eight checked by reflection against the installed assembly, parameter names included), all eleven static clip lists correctly cleared. **Board-relevant: the static layer has NO true data race** — every writer is main-thread, so the `ConcurrentDictionary`s and `PlayerPushStatesLock` are currently decorative; its bugs are lifecycle and staleness, not concurrency. Corrected `Docs\INDEX.md`: the root `.gitignore` and `.gitattributes` are **untracked**, contradicting rule 12 below. |

---

## Groomed 2026-08-16 (during board b15)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| skills-and-subagents (docs / planning) | 4 | .claude\skills\voice-line\, .claude\skills\game-update\ (+ reflect.cs), .claude\skills\ship-release\, .claude\skills\playtest\, CLAUDE.md, Docs\INDEX.md, Docs\Session_Board.md, Docs\Archive\Done\Done_2026-08.md | 2026-08-15 — **four procedure skills + subagents as standing policy.** The new skills carry STEPS (the two old ones only move the model dial). **A verification sweep by `Explore` found `CLAUDE.md`'s "adding a voice-line category is a six-place change" was WRONG — it is seven, and the missed one is `Scrap\GargoyleStatue.cs`, which holds its own parallel `clipType` switch.** Miss it and the monster taunts while the scrap is silent. `game-update` ships a working `reflect.cs` that reads any vanilla type off the installed assembly. Board-relevant: **a subagent must never claim a row, never run a build, and defaults to read-only** — the parent session owns its claim end to end. Full record in [`Done_2026-08.md`](Archive/Done/Done_2026-08.md). |

---

## Groomed 2026-08-16 (during board b14)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| csproj-portable-paths + game-compat (build + packaging, AI / state machine) | 3 | Plugin\LethalGargoyles.csproj, Plugin\src\Enemy\LethalGargoylesAI.cs, CHANGELOG.md, CLAUDE.md, ToDo.md, Docs\Session_Board.md, Docs\Archive\Done\Done_2026-08.md | 2026-08-15 — **the build is GREEN again (0 errors, 0 warnings) but it did not start that way, and the reason predates this session: THE GAME UPDATED UNDER v0.7.0.** A baseline build run *before* any edit failed on `EntranceTeleport.exitPoint`, which the game **deleted**. Settled by reflecting over the installed `Assembly-CSharp.dll` with a `MetadataLoadContext` — grep is actively misleading here, since `exitPointDoesntExist` leaves the string in the binary. Fixed via `FindExitPoint()` + `exitScript.entrancePoint`, which also adds a null guard the old code lacked. **Compile-verified only — an in-game teleport check is parked in `ToDo.md`.** csproj: `$(NuGetPackageRoot)` + a shared `$(NVorbisVersion)` replace the hardcoded nuget path; `Unity.Collections`/`Unity.Mathematics` move to `$(ManagedDirectory)` with `Private="false"`, which also stopped two game assemblies being copied into `bin\` every build. Full record in [`Done_2026-08.md`](Archive/Done/Done_2026-08.md). |

---

## Groomed 2026-08-16 (during board b24)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| statue-pocketed (scrap + items) | 13 | Plugin\src\Scrap\GargoyleStatue.cs, CLAUDE.md, Docs\Plans\Optimization_Audit.md, Docs\Archive\Done\Done_2026-08.md, Docs\Session_Board.md | 2026-08-16 — **CONFIRMED FIXED IN GAME by Mathew.** The Gargoyle Statue played one taunt per connected MACHINE (2 players = 2 overlapping voices). **Cause was b8 acting on audit finding C1, which was FALSE — and C1 is now marked WITHDRAWN in [`Optimization_Audit.md`](Plans/Optimization_Audit.md) so nobody re-applies it.** `GrabbableObject.ItemActivate` is **replicated by vanilla to every machine** (host runs it for a client-held item: `server=True owner=False heldBy=Player #1`), so the original `IsServer` guard was load-bearing and the statue was never mute for non-hosts. Reverted in `48d5e64`. **Process lesson, now in `CLAUDE.md`: the first repro had TWO multipliers both equal to 2 (statues held, players connected); this session anchored on the wrong one and shipped two wrong `isPocketed` fixes before instrumenting. Mathew changing the experiment — one single-player run, 2 statues, 1 taunt — is what solved it, not another hypothesis.** `isPocketed` was kept where it IS correct: the `Update()` dog-taunt path, which really does tick per inventory item. Also added at Mathew's request: a server-side reservation keyed by client id giving **one talking statue per player**, replacing a `!scrapAudio.isPlaying` check that could never work (PlayOneShot does not drive `isPlaying`). Full record in [`Done_2026-08.md`](Archive/Done/Done_2026-08.md). |

---

## Groomed 2026-08-16 (during board b21)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| pathfinding-audit (docs / planning) | 10 | Docs\Plans\Pathfinding_Audit.md (NEW), Docs\INDEX.md, ToDo.md, Docs\Session_Board.md | 2026-08-16 — **read-only deep dive on navigation; NO source touched, no build run.** Full record in [`Pathfinding_Audit.md`](Plans/Pathfinding_Audit.md), sixteen findings P1–P16, batches N1–N6 **proposed and unapplied.** Everything was verified by decompiling PathfindingLib 2.4.1 **and** the game's `Assembly-CSharp.dll` with `ilspycmd` — method bodies are quoted in the doc, so no claim rests on inference. **The headline: PathfindingLib is being used for the least valuable thing it does.** Its multi-destination, off-thread, true-path-length API (`StartPathTask(agent, origin, List<Vector3>, flags)` + `GetPathLength(i)`) is NEVER called, while the three searches that actually need it — `ChooseClosestNodeToPos`, `FindCoverPointsAroundTarget`, `CheckZonePath` — run synchronous `CalculatePath` on the main thread up to 24/120/16 times per call. **Three findings a later session must not lose:** (1) **`SearchForPlayers()` runs every frame from `Update`, and PathfindingLib's `StartSmartSearch` calls `StopSearch(currentSearch, clear: true)` then allocates a fresh `AISearchRoutine` and restarts both coroutines** — so the search is destroyed before it can pick a single node, the Gargoyle CANNOT ROAM, and each frame also pays a `RoundManager` node refresh plus `allAINodes.ToList()`. b8's stutter hunt never saw it because it profiled `HandleStealthyPursuitState`, which needs a target. (2) **The prefab has `AIIntervalTime: 0`, `updatePositionThreshold: 0`, `syncMovementSpeed: 0` where vanilla `EnemyAI` declares `0.2f`/`1f`/`0.22f`** — so `DoAIInterval` fires every frame AND `SyncPositionToClients` fires a position RPC to every client every frame. **Host-invisible, exactly like the batch-F audio bugs.** Unity-side, needs a bundle rebuild; `updatePositionThreshold` and `syncMovementSpeed` must move TOGETHER or clients see snapping. (3) **`GetAllowedLinks()` returns `InternalTeleports` only — the one flag with nothing behind it in vanilla** (it exists for other mods' portals). `MainEntrance`, `FireExits` and `Elevators` are all off, so **the `EntranceTeleport` arm b3 repaired is unreachable dead code and board Pending step 5 cannot pass as written** — noted on that step and in `ToDo.md`. **Verified clean, recorded so nobody re-checks:** door opening is fine (the prefab's `openDoorSpeedMultiplier: 0` is overwritten in `EnemyAI.Start` from `enemyType.doorSpeedMultiplier`, which is `10`), and `moveTowardsDestination: 0` / `movingTowardsTargetPlayer: 0` are correct for an AI that drives `agent.SetDestination` itself. **Board-relevant: `Optimization_Audit.md` is STALE** — it lists G2, D1 and D2 as unapplied when all three shipped in `c0c0189`/`a1dac4b`; the corrections table at the end of `Pathfinding_Audit.md` has the details, and **D3 is the one that is still genuinely open.** |
| pathfinding-n1-n4 (AI / state machine, config) | 11 | Plugin\src\Enemy\LethalGargoylesAI.cs, Plugin\src\Config\Configuration.cs, CHANGELOG.md, Docs\Plans\Pathfinding_Audit.md, Docs\Session_Board.md | 2026-08-16 — **applied batches N1, N2(partial) and N4 of the b10 audit: P1, P4, P6, P7, P8, P10, P11.** Debug AND Release both 0 warnings / 0 errors; Release was built deliberately, since it is the only config where the `#if DEBUG` blocks are excluded. **NOTHING HAS BEEN RUN.** In parallel Mathew applied the Unity-side half (P2/P3/P14/P15/P16) and rebuilt `gargoyleassets` — verified in the prefab YAML (`AIIntervalTime: 0.1`, `updatePositionThreshold: 1`, `syncMovementSpeed: 0.22`, `m_AutoBraking: 1`, `m_ObstacleAvoidanceType: 2`) with the bundle's mtime newer than the prefab's. **His ruling: fire-exit traversal ships as `Pathfinding > Follow Players Through Exits`, DEFAULT OFF**; mineshaft elevators got a matching toggle, also off, because that is a different decision from the one he made. **The near-miss that matters most, found while applying and NOT in the audit: `Start()`'s vanilla `StartSearch(transform.position)` had to be REMOVED, or P1's guard would have disabled smart roaming entirely.** Vanilla `StartSearch` sets `currentSearch.inProgress = true` in `Start`, so the new `inProgress` guard would have seen a search already running and `StartSmartSearch` would never have been called for the whole round — falling back to vanilla roaming with no error and no log line, i.e. a "fix" that quietly made pathing worse than v0.7.0. `HandleSearchingForPlayerState` starts it on the first tick instead; `currentSearch` is a serialized field so it is non-null with `inProgress` false at spawn. **Also found while applying P10: `isOutside = !isOutside` would have been a bug** — vanilla's `SetEnemyOutside(bool)` also calls `GetAINodes()` to repopulate `allAINodes` for the region just entered, and `isOutside` gates node selection, cover search and every same-region player check in the file, so a raw flip leaves the Gargoyle hunting players it can never reach on the wrong node set. **Deliberately NOT applied, do not silently undo:** (1) **the Elevator arm is excluded from `InvalidatePathAfterLink`** — `CanActivateDestination` returns `IsInsideElevator()` for a RIDE destination, so re-pathing mid-ride would walk the Gargoyle out of the car; (2) **P13** (smart agent never re-registers after `OnDisable`) deferred — cheap, but no evidence the game disables an enemy GameObject mid-round and the batch was already large; (3) **P5/P9/P12** deferred to N5/N6, they need the multi-destination API and a real restructure. **Noted, not fixed:** `cachedAllAINodes` is a *static* cache fed from the *per-instance* `allAINodes` that `GetAINodes()` replaces — latent, only feeds the fallback path, pre-existing. |

---

## Groomed 2026-08-16 (during board b20)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| concentus-correction (build + packaging) | 9 | Docs\Plans\Optimization_Audit.md, ToDo.md, Docs\Session_Board.md | 2026-08-15 — **REVERSES b5/b7's ruling that Concentus is an unused dependency. IT IS LOAD-BEARING — do not drop it.** Mathew caught it from the Thunderstore listing, which shows the package bundles four BCL shims beside the codec. **`NVorbis 0.10.5`'s netstandard2.0 build requires `System.Memory`** (declared in its nuspec; `CopyNVorbis` copies that exact build). The game ships no `System.Memory.dll` — 167 assemblies in `Managed\` and the only near-match is the unrelated `System.Numerics.dll` — and this mod's output contains only `NVorbis.dll`/`NVorbis.xml`. **The single copy on disk is `BepInEx\plugins\qwbarch-Concentus\System.Memory.dll`.** Dropping the dependency would leave every client unable to decode any voice line, presenting exactly like the silent-audio failures in A1/F1–F5. `System.ValueTuple`, NVorbis's other declared dependency, is covered by Unity's `mscorlib` and is not at risk. **The lesson, and the reason a whole audit agent got this wrong: it searched the tree for "Concentus", correctly found zero hits, and concluded dead — but a package's value can be the transitive assemblies it bundles, and searching for a dependency by its own name can never find that.** Real remaining defect, filed in `ToDo.md` as non-urgent: the reliance is implicit and undocumented, so a repackage upstream would break the mod with no code change here; the clean fix is to ship `System.Memory.dll` ourselves via a `CopyNVorbis`-style target and only then drop Concentus. |

---

## Groomed 2026-08-16 (during board b18)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| skills-and-subagents (docs / planning) | 4 | .claude\skills\voice-line\, .claude\skills\game-update\ (+ reflect.cs), .claude\skills\ship-release\, .claude\skills\playtest\, CLAUDE.md, Docs\INDEX.md, Docs\Session_Board.md, Docs\Archive\Done\Done_2026-08.md | 2026-08-15 — **four procedure skills + subagents as standing policy.** The new skills carry STEPS (the two old ones only move the model dial). **A verification sweep by `Explore` found `CLAUDE.md`'s "adding a voice-line category is a six-place change" was WRONG — it is seven, and the missed one is `Scrap\GargoyleStatue.cs`, which holds its own parallel `clipType` switch.** Miss it and the monster taunts while the scrap is silent. `game-update` ships a working `reflect.cs` that reads any vanilla type off the installed assembly. Board-relevant: **a subagent must never claim a row, never run a build, and defaults to read-only** — the parent session owns its claim end to end. Full record in [`Done_2026-08.md`](Archive/Done/Done_2026-08.md). |
| diagnostic-logging (docs / planning) | 7 | Docs\Plans\Diagnostic_Logging.md (NEW), Docs\Plans\Optimization_Audit.md, Docs\INDEX.md, Docs\Session_Board.md | 2026-08-15 — **docs only, no source touched.** Designed the runtime-gated diagnostic logging layer in [`Diagnostic_Logging.md`](Plans/Diagnostic_Logging.md); it becomes **Batch A** of the b5 plan and absorbs b5's old A1. **The design point that matters: `[Conditional("DEBUG")]` structurally cannot do this job** — it strips the *call*, not the surrounding code (which is how a `Stopwatch` and ~15 interpolated strings leaked into Release, audit G3/G4), and it is all-or-nothing, so getting detail means shipping Mathew a DEBUG build and it is useless for a player bug report. Replacement is levels Ã— per-subsystem categories, read from the BepInEx cfg and **live in Release**, guarded by an explicit `if (LGLog.On(cat, lvl))` because **C# 10 interpolated-string handlers need a .NET 6 BCL type and the target is `netstandard2.1`** — the guard cannot be automatic here. Carries `LGLog.Invariant`, aimed at the exact silent-desync shape the audit hit nine times, with a starting set of seven checks mapped to specific findings; the hostâ†”client clip-count check alone collapses the whole audio failure class into one log line. **Deliberately NOT included: an in-game overlay (needs a `gargoyleassets` rebuild), a second log file, and live config reload.** Also recorded **all six of Mathew's rulings** on b5's open questions in `Optimization_Audit.md` â†’ *Rulings* — that section is now authoritative over the individual finding rows. Two of them are player-facing and must ride a release with a CHANGELOG line, not a code batch: **dropping Concentus** and **the new per-clip toggles for custom voice lines**. |

---

## Groomed 2026-08-16 (during board b12)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| doc-corrections + git-structure (docs / planning + build) | 2 | .gitignore (NEW), .gitattributes (NEW), README.md, Plugin\Thunderstore\manifest.json, CLAUDE.md, ToDo.md, Docs\Session_Board.md, Docs\Archive\Done\Done_2026-08.md | 2026-08-15 — **two b1 findings fixed, one parked, and the reason it's parked outranks all three: THE GAME IS UNINSTALLED, so nothing here can be built.** `Lethal Company_Data\` has no `Managed\` folder; what's left in the game directory is mod debris Steam doesn't remove. The README's custom voice-line path was wrong and **the live install proved it** — `<game root>\Lethal Gargoyles\Custom Voice Lines\` is on disk with real user OGGs in it. `manifest.json` now matches `thunderstore.toml` (it had omitted PathfindingLib, a *hard* dependency). The csproj path fix was deliberately NOT shipped: build-affecting and unverifiable. Git: root `.gitignore` + `.gitattributes` added, a tracked `.pyc` untracked, and an `Icon.png`/`icon.png` case bug fixed that `core.ignorecase=true` had been hiding. **Index changes are STAGED, not committed.** Full record in [`Done_2026-08.md`](Archive/Done/Done_2026-08.md). |

---

## Groomed 2026-08-16 (during board b11)

Moved off the board verbatim when *Recently done* passed ten rows.

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| docs-scaffold (docs / planning) | 1 | CLAUDE.md (NEW), ToDo.md (NEW), Docs\INDEX.md (NEW), Docs\Session_Board.md (NEW), Docs\Archive\Done\Done_2026-08.md (NEW), Docs\Archive\Session_Board_History.md (NEW), Docs\Plans\README.md (NEW), .claude\agents\* (NEW), .claude\skills\* (NEW) | 2026-08-15 — **the instruction/coordination structure ported from ShippingHelper and adapted to a C#/Unity mod.** Docs only; no code touched, nothing to build. **The adaptation that matters: ShippingHelper's collision surface is one Mathew + one .accdb funnelling through a serial hand-apply step, so its board guards *unapplied staged changes*. Here Claude edits and builds directly, so the board guards the SHARED BUILD OUTPUT and the dev profile instead — hence rule 5, which has no ShippingHelper equivalent.** Three doc/code disagreements were measured while writing it and are filed in `ToDo.md` â†’ Housekeeping rather than silently fixed: the README's custom voice-line path omits the `Lethal Gargoyles` folder level, `manifest.json` and `thunderstore.toml` declare different dependencies, and the csproj carries two machine-specific paths. |

---
## Batch 17 — target-reacquire (trimmed from the board 2026-08-16)

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| target-reacquire (AI / state machine) | 17 | Plugin\src\Enemy\LethalGargoylesAI.cs, CLAUDE.md, Docs\Session_Board.md | 2026-08-16 — **`BUILD` RELEASED. Debug AND Release both 0 warnings / 0 errors, netcode patcher confirmed, Dev profile holds the DEBUG DLL. NOTHING HAS BEEN RUN.** Fixed `FoundClosestPlayerInRange`. **b16's severity claim was wrong and is corrected in *Open defects* above** — the stale-count read costs one AI tick, not the target, because `GetGargoyleTargetCounts()` recomputes every call. **The genuine defect was the `else { SetTarget(null); }`: it fired in every case EXCEPT an over-subscribed target, so a gargoyle re-entering `SearchingForPlayer` still holding a perfectly good target dropped its claim for no reason**, then tried to re-pick using counts that still held the claim it had just released. Cost was jittery re-engagement plus a window where another gargoyle could double-claim the same player, which is precisely what b8's E1 existed to prevent. **Fix: discount our own claim ONCE, up front** (`targetCounts[held]--`), after which "is this player over-subscribed" and "who is the best target" both read as *other than me*. Structure is now keep / retarget / release rather than always-drop-then-reacquire, and the target-no-longer-valid case is an explicit branch off `targetCounts.ContainsKey` (the dict is keyed on `validPlayers`, so absence IS the validity check). **`ChangeTarget` deliberately NOT given the same discount** — it asks "is this player over-subscribed across the whole pack", where our own claim SHOULD count, and discounting there would stop it rebalancing at all. **NO CHANGELOG LINE, per Mathew: the defect was introduced by b8's E1, so it never shipped**, and the existing v0.8.0 bullet about gargoyles splitting up stays accurate. **Found while verifying the deploy, now in `CLAUDE.md` → *Do-not-relearn*: `CopyToTestProfile` has NO `$(Configuration)` condition, so a Release build overwrites the Debug DLL in the Gale `Dev` profile silently** (145,920 vs 166,912 bytes). Build Debug LAST or Mathew playtests a Release DLL with every `[Conditional("DEBUG")]` line missing. |


## Batch 19 — deploy-guard (build + packaging) (trimmed from the board 2026-08-16)

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| deploy-guard (build + packaging) | 19 | Plugin\LethalGargoyles.csproj.user, CLAUDE.md, Docs\Session_Board.md | 2026-08-16 — **`BUILD` RELEASED. The Gale `Dev` profile now holds the DEBUG DLL (169,472 bytes, 12:08) — CHECK THIS BEFORE ANY PLAYTEST.** `CopyToTestProfile` is now `Condition="'$(Configuration)' == 'Debug'"`. **Proven, not assumed:** a Release build was run before and after with the profile DLL's size+mtime recorded each time; before the guard Release overwrote it, after the guard it was untouched. **The b17 finding turned out to be worse than a one-session footgun — it bit ACROSS sessions.** At 12:00 another session's Release build silently replaced the Debug DLL, so for eight minutes the profile held a Release build and any playtest run in that window would have produced a log with **zero** `[Conditional("DEBUG")]` lines, for reasons invisible to everyone involved. That is why the fix is a condition and not a "remember to build Debug last" note. **`SETUP-PROJECT.py` DOES NOT EXIST** — absent from disk AND from `git ls-files`, despite `CLAUDE.md` and the `.user` file's own header both claiming it generates that file. Both corrected in `CLAUDE.md`. **The limitation this leaves, written down rather than hidden: `.csproj.user` is git-ignored AND has no generator, so this fix exists on Mathew's machine only and a `git clean -x` erases it.** A fresh clone must hand-write the file, condition included. NVorbis copies in that target hardcode `bin/debug/` and are only correct because of the condition; noted inline. |

## Batch 20 — cornered-condition (AI / state machine) (trimmed from the board 2026-08-16)

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| cornered-condition (AI / state machine) | 20 | Plugin\src\Enemy\LethalGargoylesAI.cs, CHANGELOG.md, Docs\Session_Board.md | 2026-08-16 — **CORRECTS b18, which shipped a "cornered" escalation that was not testing cornered at all.** Debug AND Release both 0/0. **NOT RUN.** Mathew: *"he just gets aggressive if he's in my line of sight for 5 seconds, instead of treating it as a last ditch effort from being cornered"* - correct, and the root of it is a b18 reasoning error worth recording: b18 argued that the hide search "never reports failure in an open room, so line of sight is the only honest signal". **The first half was right and the conclusion was wrong.** `SetDestinationToHiddenPosition`'s RETURN VALUE is useless as a cornered signal (the `>idleDistance` branch returns `true` unconditionally - it means "I set a destination", not "I found cover"), but the information existed one level down and was being thrown away: `ChooseClosestNodeToPos` already knew whether it had found a hidden node or had to fall back to a merely-best-ranked one. **Fix: `_nodeChoiceFellBack` out of the node chooser -> `_hideFoundCover` on each hide evaluation -> cornered now requires `isSeen && !_hideFoundCover` CONTINUOUSLY, either half resetting the clock.** So walking to real cover in view of a player no longer escalates, and reaching cover clears it even if the player never looks away. **Ordering is load-bearing and must not be "tidied": `SetDestinationToHiddenPosition()` runs BEFORE `UpdateCorneredTimer()` in `HandleGetOutOfSightState`, because the timer reads the verdict that evaluation just produced** - checking first tests a stale verdict and silently restores b18's timer behaviour. `_hideFoundCover` starts `true` so he is never cornered before he has tried. b18's `_corneredAggroUntil` commit window and its `HandleOutOfAggroRange` guard are unchanged and still required. |

*(Empty as of 2026-08-15 — board b1 created the board itself. The first groom will land its section above this line.)*
