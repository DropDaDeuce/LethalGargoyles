# Session Board

**Purpose:** coordination for multiple concurrent Claude sessions on this project. **Mathew does not manage this board — the sessions do.**

**This file is the single canonical rulebook for multi-session work — `CLAUDE.md` only points here. You (the session) own this file. On your first file read/edit, read this board — it is the live availability map. Add your own claim row BEFORE touching any file, keep its status current, and move it out when the work is done. Never ask Mathew to touch the board.**

**What collides in THIS repo** (it is not the same as a hand-apply project): the same `.cs` file edited by two sessions is a real git conflict; the **shared build output** (`Plugin\bin`, `Plugin\obj`) means two builds running at once produce a mangled DLL and a nonsense error; the **single dev profile** every debug build copies into means the last build wins regardless of who ran it; and the **single Unity project** cannot be open twice.

---

## Protocol (the rules)

1. **Read before you touch.** On your first file operation of the session, read this board. **Only an *Active claims* row with status `authoring` or `building` is a LOCK** — those files belong to another session. A row marked `done`, and anything in **History**, is RELEASED. Everything not in an active `authoring`/`building` row is free.
2. **Claim atomically.** Before editing anything, add a row to *Active claims* and take your batch number by editing the counter below with an EXACT content match: `next = N` → `next = N+1`. If that exact-match edit fails, another session bumped it first — re-read the board and retry with the new number. After your claim lands, re-read the board once to confirm your row survived intact; a clobbered write must not go unnoticed.
3. **Claims are terse and canonical.** The *Files* column IS the lock: a bare list of canonical paths (`Plugin\src\Enemy\LethalGargoylesAI.cs`, `Plugin\src\Utility\AudioManager.cs`, `ToDo.md`) — never prose. A sibling must be able to find a lock by scanning for the real file name at a glance. Scope, caveats and design notes go in *Notes*.
4. **The board is the lock.** If a file you want is in an active claim, work around it. If your task can't proceed without it, tell Mathew it's blocked and on which session — don't silently edit it. Two active claims may share a file ONLY when both rows name each other explicitly and one session is the designated merge owner — keep that rare and deliberate.
5. **CLAIM THE BUILD.** `dotnet build` is not a read-only act here: it writes `Plugin\bin`, `Plugin\obj`, runs the netcode patcher over the output, and **copies into the shared Gale `Dev` profile**. Take the lock `BUILD` in your *Files* column and set your status to `building` for as long as it runs. Two concurrent builds produce a corrupt DLL and an error message that describes neither cause. If you see an active `building` row, wait — a build is seconds, not minutes.
6. **Re-check blocked files before you say "waiting."** Locks clear while you work. Right before posting any final message that reports you're blocked on (or deferred work around) a claimed file — and at natural checkpoints in a long session — re-read this board. If the blocking claim is gone or `done`, DO the deferred work now instead of reporting you're still waiting. Reporting "blocked" without a fresh board read is a protocol violation.
7. **Stale claims can be challenged.** Bump your row's *Updated* date every time its contents or status move. Sessions die (closed window, exhausted context): an `authoring` row whose *Updated* is a prior day is suspect — ask Mathew whether that session is still open. Once he confirms it's dead, move the row to History marked `abandoned`; its files are free.
8. **Pick your lane.** Prefer work inside one domain lane (below) to minimise collisions. Audio, AI and Unity-side work rarely overlap.
9. **Single-writer shared docs.** `ToDo.md`, `CHANGELOG.md`, `CLAUDE.md` and this board collide easily. Claim them like any other file, and always edit with content-anchored matches, never line numbers — they go stale the instant another session writes.
10. **Park work Mathew owes, where it can't be lost.** Code compiling is NOT the same as working. If your batch needs something only he can do — **rebuild `gargoyleassets` in the Unity Editor**, record or export a voice line, run the game and confirm behaviour, bump a version number — put it in ***Pending manual steps*** below BEFORE your row leaves *Active claims*. A note that only rides a History row gets trimmed and lost. Delete the step the moment it's confirmed done.
11. **Move your row out when you're finished.** Flip to `done`, park anything owed under *Pending manual steps*, then move the row to *Recently done* — and when that table passes ten rows, move the oldest into `Docs\Archive\Session_Board_History.md` **verbatim**. Trimming is a MOVE, never a delete: these rows carry findings that exist nowhere else.
12. **Git is real here.** Commit only when Mathew asks. If you do, one batch = one commit, and say the batch number in the message. Never commit `Plugin\bin`, `Plugin\obj`, `UnityProject\Library`, `.vs`, or `.claude\worktrees`. **Ignore coverage — corrected 2026-08-15 (b2): there are FOUR `.gitignore` files and all of them are tracked** — a root one (added by b2), plus `Plugin\`, `UnityProject\` and `AssetSources\`. b1's note here claimed there were two and that neither was tracked; both halves were wrong, off a `git ls-files | grep` whose exit status silently ate the result. **`git ls-files --error-unmatch <path>` is the check that cannot lie about whether a file is tracked** — use it instead of grepping a listing. Staging explicit paths is still the better habit, but `git add -A` from the root is no longer the hazard it was.

## Domain lanes

`AI / state machine` · `audio + networking` · `patches / soft deps` · `scrap + items` · `config` · `Unity assets (Mathew-owned)` · `docs / planning` · `build + packaging`

---

## Global batch counter

next = 44

## Active claims

**ONLY `authoring` and `building` rows live here — they are the locks.** Released rows move straight to *Recently done*; do not let them pile up here. The ShippingHelper board reached 31 rows of which 29 were already released, grew past the single-read size limit, and made its own "read this board first" rule impossible to obey. Don't repeat it.

| Session | Batch | Files (the lock — canonical paths only) | Status | Started | Updated | Notes |
|---------|-------|-----------------------------------------|--------|---------|---------|-------|

> **NO LIVE LOCKS as of 2026-08-17.** Every file in the repo is free.


## Recently done (re-sync reference — NOT locks)

Terse index of the last ten released batches, so you know what moved on disk. Older rows are verbatim in [`Docs\Archive\Session_Board_History.md`](Archive/Session_Board_History.md).

| Session | Batch | Files | Done |
|---------|-------|-------|------|
| changelog-voice (docs) | 43 | CHANGELOG.md, Docs\Session_Board.md | 2026-08-17 — **Rewrote the v0.8.0 entry to the house style b42 encodes. Mathew corrected the b42 assumption: v0.8.0 is CUT BUT NOT PUBLISHED, so the entry was still editable.** **2,187 words down to 625.** Body bullets now measure median 13 / max 17 words against the 7-13 median and 33 max of v0.0.1 through v0.7.0; the old entry was median 53 / max 138. Zero em dashes, en dashes, curly quotes or semicolons. **Nothing was invented and nothing player-visible was dropped** — every claim traces to a bullet in the old text. Five v0.8.0-only headers collapsed into the canonical set: Performance and Config File Layout became nested sub-topics under `**Changes:**` (the shape v0.4.2 already uses), New Settings became a `Configuration` sub-topic under `**New Features:**` (the shape v0.4.0 already uses), Tuning became a `Gargoyle AI` sub-bullet, and Game Compatibility became `**Game Parity:**`, which is v0.7.0's own header. **One bullet was deliberately folded rather than kept:** the old "Fixed `Diagnostics > Default Level` doing nothing" described a defect in a config section that DID NOT EXIST before this release, so as a standalone Bug Fix line it was self-referential noise. Its correct behaviour is now stated once, inside the Diagnostics feature block. **The five "Not yet verified in a live round" caveats and the two config-migration warnings moved into a `Notes` block** rather than being deleted — they cannot survive a 13-word bullet, and v0.6.1 already had a Notes section to put them in. The "Confirmed in a live round" markers on the push and cornered-aggression bullets were dropped, since verified is the default reading. **MEASURED, AND IT IS THE ONLY REASON THIS NEEDS A REPACK: BOTH CUT ZIPS CONTAIN `CHANGELOG.md`.** `thunderstore.toml` copies it (`source = "../../CHANGELOG.md"`) and `pack-nexus.ps1` copies it (`$srcChanges`). So `Plugin\Thunderstore\Packages\` and `Plugin\Nexus\Packages\` both still hold the 2,187-word version. Parked as pending step 12. **The DLL, the bundle and the voice lines are untouched by this batch**, so the repack is a packaging re-run, not a re-verification of b41's content checks. |
| changelog-voice (docs) | 42 | .claude\skills\changelog-writer\SKILL.md, CLAUDE.md, Docs\Session_Board.md | 2026-08-17 — **Mathew: "0.8.0 is way too wordy." Re-voiced the `changelog-writer` skill to the pre-0.8.0 house style. Docs only, no code, no build.** The skill had been teaching v0.8.0's shape (plain sentence, then the mechanism, then the consequence) as if it were the house voice; **it was invented in v0.8.0 and matches nothing before it.** Measured off the file rather than argued: across v0.0.1–v0.7.0, **144 bullets, median 9 words, median 1 sentence, longest 33 words**; v0.8.0 is 35 bullets, **median 53 words, median 3 sentences, longest 138**. Those four numbers are now IN the skill as its target table, so a future session re-checks them instead of re-deriving the style. Other measured facts baked in: the prior file uses verb-first past tense (`Added` 25, `Fixed` 17, `Updated` 9, `Optimized` 6), third person, **second person exactly twice in the whole pre-0.8.0 file**, and **zero em dashes, en dashes, curly quotes or curly apostrophes** — so the punctuation bans were already being obeyed and are now stated as a fact about the file, not a preference. **Header sprawl is the second half of the problem:** the canonical set is New Features / Changes / Bug Fixes / Documentation / Notes, with four one-off headers in thirteen releases; v0.8.0 minted five new ones in a single release. New settings belong under New Features, tuning under Changes. **Where the honesty items went, since they cannot survive a 9-word bullet:** "Not yet verified in a live round", "your existing config keeps the old value" and "this will look like a bug but is intended" now go in a `**Notes:**` block at the bottom of the release, one line each — which is a shape v0.6.1 already uses, not an invention. The skill also carries a compression table built from four real v0.8.0 bullets cut to house style, so the next session shortening one has a worked example. **`CLAUDE.md`'s Procedure Skills bullet for this skill was carrying the same wrong description and was corrected in the same batch.** **NOT DONE, and it is Mathew's call: `CHANGELOG.md` itself is untouched.** v0.8.0 is published metadata now (both packages cut 2026-08-17, b41) and rewriting a shipped release note is a different decision from fixing the skill. If he wants it, the entry compresses to roughly 25 one-line bullets plus a Notes block. |
| release-v080 (build + packaging) | 41 | Plugin\LethalGargoyles.csproj, Plugin\Thunderstore\manifest.json, Docs\Session_Board.md | 2026-08-17 — **`BUILD` RELEASED. v0.8.0 IS CUT. Release 0 warnings / 0 errors, netcode patcher confirmed, BOTH storefront packages built and inspected. NOT PUBLISHED — that is Mathew's step and he does Nexus first.** He called the release himself: ship as-is, patch if a bug turns up. **All three version numbers now read `0.8.0`** (csproj `<Version>`, `manifest.json` `version_number`, `CHANGELOG.md` heading). **`thunderstore.toml` carries NO version of its own** — `PackThunderstore` passes `--package-version $(Version)` from the csproj — so the "three numbers" rule is really two files plus the changelog, and hunting for a version in the toml is wasted effort. **Committed the whole backlog first so the release was cut from a clean tree**, which took five thematic commits rather than one-per-batch: about fifteen batches had accumulated sharing files, `LethalGargoylesAI.cs` alone carried eleven, and a per-batch split would have produced commits that do not compile. **Both zips verified with Python, not .NET** (b28's finding: `ZipArchiveEntry.FullName` rewrites `/` to `\` as it reads on .NET Framework, so a separator check built on it calls a correct archive malformed). Results: Thunderstore 179 entries rooted at `plugins/`, Nexus 180 rooted at `BepInEx/plugins/` plus `fomod/`, `Optional/Coroner/` and the manual-install readme; **170 voice clips across 10 categories in each; packaged DLL 154,112 bytes, which is the PATCHED one (the `_original` is 150,016), so the netcode pass is genuinely inside both packages**; bundle byte-identical to the working tree at 1,786,299; zero backslash path entries in either. `pack-nexus.ps1` reported `fomod validates as XmlScript 5.0`, so b31's Vortex version-detection check passed. **Shipping with five changelog bullets still marked "Not yet verified in a live round" — Mathew's explicit, informed decision, not an oversight.** Also still true and deliberately shipped: `taunt_employeeclass_Scout.ogg` is the one stereo clip in the set (verified from the Vorbis header: 2ch, 44.1kHz), which the b-F1 decoder fix handles, so the mono re-export is robustness rather than a blocker. |
| nexus-link-swap (docs) | 40 | README.md, Plugin\Nexus\Nexus_Description.bbcode, Docs\Session_Board.md | 2026-08-16 — **Closes pending step 15 — Mathew supplied the Nexus page: `https://www.nexusmods.com/lethalcompany/mods/309`.** Docs only, no build, nothing repacked (the Nexus zip does not contain either description). `README.md` is the Thunderstore page body, and its *Feedback and Requests* section now points at the Nexus page **with the Posts tab named explicitly** — his stated intent was *“I want them to know they can reach me on that forum”*, so a bare link would have missed the point. The Nexus description got the mirror of it: a *Right here — post in the Posts tab on this page* bullet, since linking a page to itself would be silly. **Flagged to him and NOT a defect: the page returns “Mod unavailable” to an anonymous fetch**, which is the normal state for a Nexus page that is still unpublished. It becomes a live link the moment he publishes. **The ordering risk that creates: a Thunderstore release shipped BEFORE the Nexus page goes public would put a dead link on the Thunderstore page**, so publishing the Nexus page is now a prerequisite of the next release, recorded in *Pending manual steps*. The Discord link is gone from both descriptions. |
| drop-softdep-marketing (build + packaging, docs) | 39 | README.md, Plugin\Nexus\Nexus_Description.bbcode, Plugin\Nexus\MANUAL-INSTALL.txt, Plugin\Nexus\fomod\ModuleConfig.xml, Docs\Session_Board.md | 2026-08-16 — **Mathew's call: Employee Classes and Enhanced Monsters are untested and he does not plan to keep supporting them, so their entries come out of BOTH storefront descriptions. Coroner stays. No removal note, by his instruction.** Repacked, validates as XmlScript 5.0. **DESCRIPTIONS ONLY — deliberately no code change.** All three `SoftDependency` attributes are untouched, the five `taunt_employeeclass_*.ogg` clips still ship in the package, and they still play for anyone who happens to run the mod. This de-advertises the integrations; it does not remove them. **Removed:** both Compatibility entries (Nexus + README), the Employee Classes block in *Current Voice Lines* and its index line, the `(Requires the Employee Classes mod)` category parenthetical, the optional-extras mentions in the description, the zip readme and the FOMOD page, and the public roadmap line *1 more taunt for each employee class* from `README.md`'s To Do. **Deliberately LEFT, and flagged to him rather than decided silently: the *Hardcoded Categories* sentence in both descriptions still names `Taunt - EmployeeClass`.** That is custom-voice-line mechanics, not a compatibility claim — the folder is still created at runtime and the naming rule is still true — so removing it would have cost real instructions for no gain. **Also NOT touched, because he did not ask and it is his backlog: `ToDo.md` still carries *One more taunt for each employee class*.** If support is genuinely dropped that item is dead and should go. **Second change, from his correction: Vortex installs BepInEx itself**, so it is now six Thunderstore downloads, not seven — the BepInEx link is marked skip-if-using-Vortex in all three copies and step 2 says so. **And MO2 is no longer claimed as supported:** he does not use it and will never test it, so the wording is now *should work, untested*, with an invitation to report back. |
| nexus-install-instructions (build + packaging) | 38 | Plugin\Nexus\Nexus_Description.bbcode, Plugin\Nexus\MANUAL-INSTALL.txt, Plugin\Nexus\fomod\ModuleConfig.xml, Docs\Session_Board.md | 2026-08-16 — **Mathew's observation, and it invalidated the framing of the three batches before it: “if someone is installing my mod, they WILL be manually installing mods.”** Repacked, still validates as XmlScript 5.0. **Two things b37 got wrong.** (1) `MANUAL-INSTALL.txt` opened with *“if you used Vortex or MO2 you can close this file”* — false, because a mod manager installs THIS mod and none of the six Thunderstore-only libraries. It now opens by telling Vortex users explicitly to keep reading. (2) b37 made **r2modman/Gale the recommended path for the library stack. That is actively wrong advice for a Nexus user: those managers keep mods in a PROFILE folder and launch the game against it, NOT the game folder Vortex deploys into**, so following it would leave the two halves invisible to each other. The mod-manager option is now framed as an either/or — use it for everything including the Thunderstore build of this mod, or do it all by hand — never as a mix. **The full seven-mod guide now sits at the TOP of the Nexus description, directly under the AI disclaimer**, replacing the old Requirements and Installation sections rather than duplicating them; the old ones were deleted so they cannot drift apart. **Per-package destinations were measured by unzipping all seven, not assumed, and they are genuinely irregular:** LethalLib ships a `plugins/` folder, HookGenPatcher `patchers/` + `config/`, MonoDetour `core/`, MonoDetour_BepInEx_5 `core/` + `patchers/`, **PathfindingLib is a bare loose DLL**, and **Concentus is two folders plus four loose `System.*.dll` files** with no wrapper folder at all. A single blanket instruction would have broken at least three of them. All seven Thunderstore URLs were confirmed live against the API before being written into a public mod page. **Still owed and parked below: the Thunderstore README still points at the Discord channel and Mathew wants it pointing at the Nexus page — needs the URL, which does not exist yet.** |
| nexus-dep-tree (build + packaging) | 37 | Plugin\Nexus\fomod\ModuleConfig.xml, Plugin\Nexus\Nexus_Description.bbcode, Plugin\Nexus\MANUAL-INSTALL.txt, Plugin\Nexus\README.md, Docs\Session_Board.md | 2026-08-16 — **Mathew asked two questions and both had a bigger answer than they looked. Repacked, still validates as XmlScript 5.0.** **(1) MMHOOK is not the only missing transitive dep - there are THREE, and b36's install wording broke all of them.** Measured off the Thunderstore API and by unzipping the packages, not assumed: LethalLib needs `Evaisa-HookGenPatcher` (MMHOOK) AND `MonoDetour-MonoDetour_BepInEx_5`, which itself needs `MonoDetour-MonoDetour`. **Crucially they do NOT install to `plugins`** - HookGenPatcher unpacks to `BepInEx\patchers\` plus a config file, and the two MonoDetour packages to `BepInEx\patchers\` and **`BepInEx\core\`**. So b36's *drop their plugin folders into BepInEx\plugins* would have silently broken every one of them. **The fix is not a longer list** - enumerating a dependency tree that just changed (MonoDetour is a recent addition) ages badly. All three copies now steer players to **r2modman or Gale for the library stack**, which resolves the whole chain and files each piece correctly, with a careful manual route as fallback that says outright which folder each package targets. **(2) Bundling the dependencies: REJECTED, and written up so it is not revisited.** LethalLib and PathfindingLib are MIT so redistribution is legal - that is the weakest part of the question. The real objections: a bundled copy **collides with the Thunderstore copy a player already has** (duplicate plugin GUIDs, mismatched assemblies in `BepInEx\core`), it **freezes them on our shipped version** and breaks other mods needing newer, and **HookGenPatcher/MonoDetour live in BepInEx's OWN `patchers\` and `core\` folders**, which our installer has no business writing. Etiquette agrees: in BepInEx-land you declare deps, you do not ship them. **`NVorbis` is the instructive exception** - a plain NuGet library, no plugin GUID, inside our own folder, which is exactly the kind that IS fine to bundle. Also tidied two things b32/b36 left behind in `ModuleConfig.xml`: an orphaned paragraph in the schema comment, and a step title/comment still saying *two hard dependencies* when it is four. |
| nexus-deps-complete (build + packaging) | 36 | Plugin\Nexus\fomod\ModuleConfig.xml, Plugin\Nexus\Nexus_Description.bbcode, Plugin\Nexus\MANUAL-INSTALL.txt, Plugin\Nexus\README.md, .claude\skills\ship-release\SKILL.md, Docs\Session_Board.md | 2026-08-16 — **Mathew caught b33's requirements list as incomplete, and he was right: it named three, the real answer is FOUR required plus three optional.** Repacked and re-validated as XmlScript 5.0. **The missing one is Concentus, and the reason it was missed is the lesson:** only `LethalLib` and `PathfindingLib` are hard `[BepInDependency]` attributes, so they are the only two BepInEx names in the `missing dependencies` log line — and b33 built the list from that error plus the source. **`qwbarch-Concentus 2.3.0` is declared in `thunderstore.toml` and `manifest.json` and appears NOWHERE in `Plugin\src`**, so both the log and the code undercount it. **`thunderstore.toml` is the only complete list; take it from there.** Now recorded that way in `ship-release`. Also added the three **optional** soft deps (Coroner, Employee Classes, Enhanced Monsters) to all three requirement copies, since b33 mentioned them only in the description's Compatibility section, and a line saying **NVorbis is not a user install** — it ships inside the package under `lib\NVorbis\`, and it is the obvious next thing for someone to wrongly add to the list. **Parked, unchanged, and explicitly Mathew's: nothing references Concentus** (the only hits in the plugin tree are the two declarations, so it is probably vestigial from an earlier audio codec). It is listed as **required** because that is what the published package declares and because he confirmed it — dropping a declared dependency is player-facing, not a cleanup. `CLAUDE.md` has carried this open question since 2026-08-15 and it is still open. |
| nexus-infoxml-corroboration (build + packaging) | 35 | Plugin\Nexus\README.md, Docs\Session_Board.md | 2026-08-16 — **Docs only. Corroborates b33 from Nexus's own documentation and changes no conclusion — recorded because the CONTRADICTION it found is a trap.** Nexus states it outright in the *About Fomod* page of the Nexus Mods app docs: `info.xml`'s metadata *"is redundant to what Nexus Mods reports through the api"*, so *"Vortex at least largely ignores this file."* **But Nexus's own user-facing *How to create mod installers* wiki page says the opposite** — that `info.xml` and `ModuleConfig.xml` *"both contain information Vortex will load during the installation process."* **That sentence is wrong, and it is the more discoverable of the two**, so a future session that reads only the installer guide would go hunting for a bug in `info.xml` that does not exist. Both are now quoted in `Plugin\Nexus\README.md` with the guide explicitly flagged as unreliable. Two closed Vortex issues (#11776, #4420) asked for exactly the behaviour that guide describes. **Also captured, practical:** the Source dropdown that clears the "no source assigned" warning lives in the **right-hand mod detail pane**, not the table — its column is `isDefaultVisible: false`, which is why it is hard to find; and once the mod is published, **Query Info** (on a download) and **Fix missing IDs** (on an installed mod) can retro-fit real metadata onto a manually-added copy via an MD5 lookup against the Nexus meta server. No package change, nothing repacked. |
| nexus-requirements-refinement (build + packaging) | 34 | Plugin\Nexus\README.md, .claude\skills\ship-release\SKILL.md, Docs\Session_Board.md | 2026-08-16 — **Docs only, no package change, nothing repacked.** Refines b33 off a deeper read of Nexus's own documentation, and **corrects one thing b33 overstated.** b33 concluded the Requirements tab was "documentation, nothing more" — **half wrong. Vortex ignores it, but nexusmods.com itself prompts users to grab listed requirements when they click download** (*Best Practices for Mod Authors*), so filling it in has real reach, just not through Vortex. That distinction is now explicit in `ship-release`, along with the exact editor box to use: **"Other required resources"** (name / URL / notes), which is separate from the "Required mods on the Nexus" search — that search **will not find LethalLib or PathfindingLib and that is expected**, not a mistake to go hunting for. **Two automation routes were checked and rejected, so a later session does not re-investigate them:** (1) the **`.vdeps`** file used by the third-party *Mod Dependency Fulfiller* extension only resolves dependencies **downloaded through Nexus**, which ours by definition are not; (2) a **Vortex Collection** genuinely can automate an external download (Direct Download, or a guided Browse-a-website step) and is the ONLY mechanism that can — but it is a separate product to publish and maintain, not a release step, so it is recorded as an option rather than adopted. |
| fomod-metadata (build + packaging) | 33 | Plugin\Nexus\fomod\ModuleConfig.xml, Plugin\Nexus\Nexus_Description.bbcode, Plugin\Nexus\MANUAL-INSTALL.txt, Plugin\Nexus\README.md, .claude\skills\ship-release\SKILL.md, Docs\Session_Board.md | 2026-08-16 — **b31/b32 CONFIRMED WORKING — Mathew installed through Vortex and the FOMOD ran correctly.** This batch answers the two things he saw next, and **the headline is that one of them needed NO code change and the other could not be fixed in code at all.** (1) **Vortex listing the mod as `LethalGargoyles-0.7.0.zip` with a blank Version and "no source assigned" is the local-install state, not a defect.** `fomod\info.xml` does not drive either field: Vortex 1.x read `<Name>` from it and **2.x has that extractor commented out** (`installer_fomod_shared/index.ts`, *"not worth the hassle"*), and no version ever read `<Version>`. For a local zip the name falls through to the archive filename and version/source are simply undefined; **Mod Manager Download stamps all three from the Nexus API**, so what actually matters is the Name and Version fields on the upload page. Recorded in `ship-release`. **Explicitly rejected: `vortex_override_instructions.json`**, which would force it and would also change the installer's stop-pattern handling and null `pluginPath` — internal machinery, not an author contract. (2) **His `missing dependencies` BepInEx error exposed a real publishing problem.** **Neither hard dependency is on Nexus** — verified by enumerating all 181 Lethal Company mods through the Nexus API, not off a search snippet (a snippet claimed LethalLib was mod 76; **mod 76 is an unrelated MelonLoader mod by a different author**). Worse, **Vortex does not read a mod page's Requirements tab at all** (Vortex issue 16360), so no user is ever prompted. **And the plugin cannot warn either — a subagent recommended detecting the missing GUIDs in `Awake`, which is wrong: hard `[BepInDependency]` entries mean BepInEx refuses to load the assembly, so our code never runs.** BepInEx's own log line is the only signal that exists. **The one surface we control is the installer**, so the FOMOD now opens with a read-only notice page (`SelectAll` + a single `Required` fileless plugin) naming all three dependencies with Thunderstore URLs; the description and `MANUAL-INSTALL.txt` carry the same list. **That makes requirements a THREE-copy hand-maintained fact** on top of the toml and manifest — written up in `Plugin\Nexus\README.md`. **Corrected in `ship-release`, which b28 had wrong:** it claimed the Requirements tab "is the only thing Vortex reads for dependencies". It reads nothing. Also noted, not acted on: **Thunderstore's current LethalLib is 1.2.0 while `thunderstore.toml` declares 1.1.1** — harmless, since the declaration is a minimum, and bumping a declared dependency is Mathew's call. Repacked and re-validated; no source touched, no build needed. |

## Open defects (unclaimed — free to pick up)

**NONE OPEN as of 2026-08-16.** b16's `FoundClosestPlayerInRange` entry is **FIXED in b17** and the row is in *Recently done*.

**b16's write-up of that defect was WRONG in its severity and is corrected here so nobody re-derives the scary version.** It claimed a gargoyle "can never re-acquire the player it just dropped", predicting a solo-play softlock. That does not follow: `FoundClosestPlayerInRange` calls `GetGargoyleTargetCounts()` fresh on **every** invocation, so the tick after `SetTarget(null)` lands the counts are recomputed without our claim and the player is selectable again. The stale-count read cost **one AI tick (0.1s)**, not the target. **The lesson: a static read of "the count still says 1" is only a bug if nothing recomputes the count — check the caller's loop before calling something a blocker.**

> **Verify a defect against the code before acting on this section.** A stale row here is worse than an empty one: on ShippingHelper a "fix still owed" row sat here after the fix had shipped, and it cost a whole session re-fixing an already-fixed bug.

## Pending manual steps

Things Mathew owes before a batch is actually live (rule 10). Park a step here BEFORE its claim row leaves *Active claims*; **delete the line the moment it's confirmed done.**

**b2 steps 2-4 are DONE, confirmed 2026-08-15:** `git gc` ran (3,053 loose objects → one 699.53 MiB pack, zero loose), the stale worktree is removed, and the game is reinstalled with `Managed\` present. Two steps remain.

1. ~~**Commit b2 and b3.**~~ — **DONE 2026-08-17 (b41).** The entire backlog is committed; `HEAD` is now the v0.8.0 bump and the tree is clean. The split ended up thematic rather than per-batch, because roughly fifteen batches had accumulated sharing the same files and `LethalGargoylesAI.cs` alone carried eleven of them, so a per-batch split would have produced commits that do not compile. Original text kept for context. `HEAD` was `decb0d8` and **b2's index changes are staged but never committed** — `git rm --cached` (the stray `.pyc`) and `git mv -f Icon.png icon.png`. Until that lands, the filename-case fix is not real, and `core.ignorecase=true` will keep hiding it. b3's source changes sit on top, uncommitted. **Suggested split, since deletions/renames and code changes should stay attributable: one commit for b2 (git hygiene + docs), one for b3 (csproj + the game-compat fix), one for b6 (the icon).** Note `icon.png` now shows as `AM` — b2's staged `git mv` plus b6's new content on top; both land together. **ADDED BY b5: the root `.gitignore` AND `.gitattributes` are still UNTRACKED** — `git ls-files --error-unmatch` fails on both, so rule 12's "all four are tracked" is wrong. **Explicitly `git add` them in the b2 commit.** This is not cosmetic: the root `.gitattributes` is what declares `gargoyleassets` binary, and that file has **no extension**, so with `core.autocrlf=true` a fresh clone elsewhere decides text-vs-binary by sniffing content and can inject CRLF into the asset bundle.
2. ~~Triage the audit's six open questions~~ — **DONE 2026-08-15 (b7).** All six ruled; answers are in `Optimization_Audit.md` → *Rulings*, which is authoritative over the individual finding rows. Batches A–G remain **proposed and unapplied**; the batch order is Mathew's to start, not a step he owes.
3. **PLAYTEST b8 — five commits of unrun code are now sitting in the tree.** Batch A first (start the game, confirm the log is quiet apart from the session banner, then set `[Diagnostics] StateMachine = Debug` and confirm a readable transition trail). Then a real round. **Any `INVARIANT VIOLATED` line is a pre-existing bug being surfaced for the first time, not a regression — send it over.** The behaviour most likely to look "wrong" but be right: gargoyles now SPREAD ACROSS the crew instead of dogpiling one player (E1), and the gargoyle sometimes circles the other way (E2). Details in [`Optimization_Audit.md`](Plans/Optimization_Audit.md).
4. **Re-export `taunt_employeeclass_Scout.ogg` as MONO (Audacity).** It is the only stereo file in the shipped set, and the client-side NVorbis decode halves stereo clips — so that line is currently cut off mid-sentence for everyone except the host. The decode bug is fixed in code by batch F1, but **the file should be mono regardless**, since every other clip is and the pipeline is only ever exercised for mono.
5. ~~**Play a round and watch the Gargoyle use an entrance.**~~ — **BLOCKED, do not attempt yet. Found 2026-08-16 (b10).** The `SmartDestinationType.EntranceTeleport` arm b3 repaired is **unreachable**: `GetAllowedLinks()` returns `SmartPathfindingLinkFlags.InternalTeleports` only, so PathfindingLib will never hand the Gargoyle an entrance-teleport destination. Nothing is wrong with b3's fix — it simply cannot execute. Batch **N4** of [`Pathfinding_Audit.md`](Plans/Pathfinding_Audit.md) has to land first, **including P10**, or the Gargoyle will ping-pong through the fire exit forever. Keep this step; it becomes valid the moment N4 ships.
6. ~~A pathfinding audit is now open and unstarted~~ — **SUPERSEDED by step 7. N1/N2(partial)/N3/N4 all landed 2026-08-16 (b11).** The design question is answered: fire-exit traversal ships as a config toggle, **default off**. N5/N6 (P5, P9, P12) and P13 remain proposed.
7. **PLAYTEST b11 — the pathfinding batch. This is the biggest behaviour change since v0.7.0 and NOT ONE LINE OF IT HAS RUN.** Both halves landed together: the C# fixes are in the tree (Debug + Release, 0 warnings / 0 errors) and the Unity-side prefab values are in the rebuilt `gargoyleassets`.
   - **Solo host round first.** The headline is that **the Gargoyle should now actually patrol while searching** instead of loitering near its spawn — it never roamed at all before, because its search restarted every frame. Expect it to find you noticeably sooner and to feel more present. If it now feels *too* aggressive, that is a tuning conversation (`Awareness`, `Spawn weight`), not a regression.
   - Watch for it **braking as it arrives** somewhere instead of overshooting and shuffling — that is `m_AutoBraking`.
   - Set `[Diagnostics] Movement = Debug` and you should see `starting smart search (links=InternalTeleports)` **once per stint** in the search state. **If that line repeats every frame, the P1 guard did not take** — send it over.
   - **Then a real multiplayer lobby, and this part cannot be skipped or self-tested.** `updatePositionThreshold` went 0 → 1 and `syncMovementSpeed` 0 → 0.22. Ask whoever joins whether the Gargoyle moves *smoothly* for them. **Snapping or teleporting in ~1 m steps means those two need rebalancing, and it is invisible on your machine.**
   - **Leave both new `Pathfinding` settings OFF for the first few rounds.** Establish the baseline first. When you do try `Follow Players Through Exits`, the thing to watch is that the Gargoyle **passes through the fire exit once and keeps going** — if it ping-pongs in and out of the doorway, `InvalidatePathAfterLink` is not firing and that is the bug to report. Trying that toggle is also what finally makes step 5 above testable.
8. **CONFIRM THE b14 CONFIG MOVE — it needs ONE game launch and it cannot be checked any other way.** The .cfg is written by the game, not by the build, so nothing has actually reordered on disk yet. Start the game once, quit, then open `BepInEx\config\DropDaDeuce.LethalGargoyles.cfg`.
   - **`[General]` should now be within the first ~110 lines instead of line 939**, and the `[Voice Lines.*]` sections should all be below `[Scrap]`. There should be **no `[Audio.*]` sections left at all**.
   - **The risk this step exists to catch:** the migration reads BepInEx's private `OrphanedEntries` to carry old per-clip on/off values across the rename. **Mathew's own cfg has ZERO disabled clips right now (measured 2026-08-16), so his machine cannot prove the migration works** — it will report nothing and look fine either way. To actually test it: set a couple of `Enable <clip> = false` under the OLD `[Audio.*]` names *before* launching, then confirm they come back as `false` under `[Voice Lines.*]`. With `[Diagnostics] Config = Info` the log says `Moved N disabled voice-line toggle(s)`.
   - If instead the log carries `BepInEx's private OrphanedEntries property was not found`, the reflection broke and disabled clips will silently re-enable — that is the one failure mode worth reporting.
9. **PLAYTEST b17's targeting fix. One solo round is enough to catch the regression; the thing it actually fixes needs two Gargoyles.** Debug build is already in the Gale `Dev` profile.
   - **REWRITTEN 2026-08-16 (b19). Mathew ran the first version and said "I think I have it tested? Hard to tell for me" — that is a defect in the instruction, not in his testing** (see the `playtest` skill). The original asked him to judge whether re-engagement "felt normal", which is not observable by a human watching one Gargoyle. **Do not ask for a feel judgment on this fix; the log is the only decisive evidence and a session must read it, not him.**
   - Setup: `[Diagnostics] Enabled = true` and `[Diagnostics] Targeting = Debug` in `BepInEx\config\DropDaDeuce.LethalGargoyles.cfg`. Solo round, any moon.
   - Procedure: get found, run away until it loses interest, come back. Twice. Then quit and hand over `LogOutput.log`.
   - **What a session reads it for.** PASS: `released target ... (overSubscribed False, inRange False)` when he outruns it, then a matching `acquired target` when he returns. FAIL, and it is the b17 regression: `released` with no `acquired` afterwards despite him being back in range. FAIL, the old pre-b17 churn: `acquired`/`released` for the same player repeating many times a second.
   - **Multi-Gargoyle spread is a SEPARATE test and needs 2+ Gargoyles AND 2+ players.** It cannot be inferred from a solo log. Do not report the b17 fix as verified off the solo round alone; the solo round only proves no regression.
10. **PLAYTEST b23's footstep-volume fix. Second machine required, same as b22 — the host has always sounded correct and still will, so a solo round proves nothing at all.** Debug build is already in the Gale `Dev` profile. **b22's push fix is confirmed working and needs no further testing.**
   - Procedure: Mathew hosts, someone else joins. The joining player listens to the Gargoyle's footsteps across a full round, and specifically while it creeps up to shove them.
   - PASS: on the CLIENT, the footsteps now change with what he is doing. Near-silent as he closes in for a shove, silent while he stands watching, quieter while stalking, loud only in an open chase or while he is searching.
   - FAIL: the client hears one flat volume no matter what he is doing, which is the old behaviour.
   - **The pushed player is the one who has to judge this**, not the host. Nothing about the host's audio changed in this batch.
   - Worth knowing while listening: `AggressivePursuit` is authored at `1.7` and Unity clamps audio volume to `1`, so a chase is already at maximum and cannot get louder. If the gap between creeping and chasing still reads as too small, the lever is lowering the other values, not raising that one.
11. ~~**THE THREE VERSION NUMBERS NOW DISAGREE**~~ — **RESOLVED 2026-08-17 (b41). All three read `0.8.0`**, Mathew called the release, and both packages are cut. *Open defects* was empty at the time, so the gate this step named was satisfied. Original text follows for context. b16 did that deliberately. `CHANGELOG.md`'s top heading says **`## v0.8.0 - The Wandering Gargoyle`** (Mathew's call, 2026-08-16); `Plugin\LethalGargoyles.csproj` `<Version>` and `Plugin\Thunderstore\manifest.json` `version_number` both still say **`0.7.0`**. Nothing enforces agreement and a mismatch does not fail the build — it ships a package whose own metadata contradicts itself. Bumping a version is a release act and it is Mathew's, so a session did not do it unasked. **Load the `ship-release` skill when he calls for it; do not bump one number in isolation.** Note also that a v0.8.0 release should not go out while *Open defects* above is non-empty, and it currently is.
   - Also worth a listen while you are in there: `General > SteamID Taunt Cooldown` is new (default 90, the old hardcoded value), and the taunt defaults moved to 30–60. **Your existing cfg keeps 15/45** unless you delete those two lines.
12. **REPACK BOTH v0.8.0 ZIPS BEFORE PUBLISHING — they carry the OLD changelog.** Added 2026-08-17 (b43). Both packages copy `CHANGELOG.md` into themselves (`thunderstore.toml` `[[build.copy]]` with `source = "../../CHANGELOG.md"`, and `pack-nexus.ps1` `$srcChanges`), and b43 rewrote the v0.8.0 entry from 2,187 words to 625 **after** b41 cut them. The zips are otherwise correct: **no code, no bundle and no voice line changed in b43**, so this is a packaging re-run and b41's content inspection (170 clips, patched DLL at 154,112 bytes, bundle byte-identical, zero backslash entries) still stands. One `dotnet build -c Release` regenerates both, since `PackThunderstore` and `PackNexus` run off the same `bin\Release`. **A session can do this, but it needs the `BUILD` lock.** `CopyToTestProfile` is Debug-only since b19, so a Release build will NOT clobber the Debug DLL in the Gale `Dev` profile. **Alternatively, skip it deliberately:** the bundled changelog is a convenience copy, and the text players actually read on either storefront is the Thunderstore README or the Nexus BBCode description, neither of which contains the changelog. Mathew's call, but do not let it be an accident.
13. **PUBLISH v0.8.0 — BOTH PACKAGES ARE CUT AND WAITING, NEXUS GOES FIRST.** `Plugin\Nexus\Packages\LethalGargoyles-0.8.0.zip` and `Plugin\Thunderstore\Packages\DropDaDeuce-LethalGargoyles-0.8.0.zip`, both built 2026-08-17 from the same `bin\Release`, both inspected (170 clips, 10 categories, netcode-patched DLL at 154,112 bytes, bundle byte-identical to disk, zero backslash path entries). **Mathew publishes Nexus first, then updates the Thunderstore page** — that ordering is his call and it is what defuses step 15. Step 11's version blocker is resolved. The `ship-release` skill carries the full upload checklist. The short version: upload the zip, **set the file's Name field to `Lethal Gargoyles` and its Version field to the release version** (b33: those two Nexus fields are what fill the Name/Version columns in every user's Vortex — `info.xml` does not), paste `Plugin\Nexus\Nexus_Description.bbcode` into the description box **as BBCode** (Nexus does not render Markdown), fill the **Requirements** tab — LethalLib and PathfindingLib go in the **"Other required resources"** box with Thunderstore URLs, since neither is on Nexus (b33/b34: **Vortex ignores that tab entirely, but the nexusmods.com website DOES prompt people to grab listed requirements when they click download**, which is why it is worth doing), and tick the AI-generated-content box to match the README's disclosure.
14. **~~Re-test the b31 fomod fix~~ — DONE, CONFIRMED IN VORTEX by Mathew 2026-08-16. The installer runs correctly.** What remains is only the **b33 requirements page**, which he has not seen yet: rebuild, reinstall, and check that the FIRST installer page is a read-only notice listing BepInEx / LethalLib / PathfindingLib with the Thunderstore links, followed by the Coroner page as before. **Everything else on that screen was diagnosed and needs no action** — mod named `LethalGargoyles-0.7.0.zip`, blank Version, "no source assigned" are all the local-install state and resolve themselves for anyone using Mod Manager Download. Original (now closed) step follows for context: install `LethalGargoyles-<ver>.zip` in Vortex and confirm you get a checkbox for "Coroner death messages", then that the game folder ends up with `BepInEx\plugins\LethalGargoyles\` containing the DLL, `gargoyleassets` and the whole `Voice Lines` tree. **If the installer misbehaves it is not urgent:** the zip is built so a manual drag of its root `BepInEx` folder is a complete install, which is why this is a verification step and not a blocker. What WOULD matter is Vortex nesting it wrong — `BepInEx\plugins\BepInEx\...` or `BepInEx\plugins\LethalGargoyles\BepInEx\...` — so that is the specific thing to look at.
15. **DECIDE whether "now on Nexus Mods" earns a `CHANGELOG.md` line.** A session did not write one, because nothing is published yet and the changelog is for things that are true. If the v0.8.0 release goes out on both storefronts, it is worth one plain sentence — load `changelog-writer` for it.
16. **PUBLISH THE NEXUS PAGE BEFORE (OR WITH) THE NEXT THUNDERSTORE RELEASE — b40 created an ordering dependency that did not exist before.** `README.md` is the Thunderstore page body and it now links `https://www.nexusmods.com/lethalcompany/mods/309`. That page currently returns **“Mod unavailable”** to anyone not signed in as its author, which is normal for an unpublished Nexus page — but it means **a Thunderstore release cut before the Nexus page is public would put a dead link in front of every Thunderstore player.** Either publish the Nexus page first, or pull that bullet back out before releasing. Nothing enforces this.

## History

Released rows older than the last ten live in [`Docs\Archive\Session_Board_History.md`](Archive/Session_Board_History.md), verbatim.
